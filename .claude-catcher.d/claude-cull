#!/bin/bash
f="$0"; while [ -L "$f" ]; do d="$(cd "$(dirname "$f")" && pwd)"; f="$(readlink "$f")"; [[ "$f" != /* ]] && f="$d/$f"; done
exec "$(cd "$(dirname "$f")" && pwd)/../claude-catcher" cull "$@"
